# Anthropic API key (required)
anthropic.api-key=${ANTHROPIC_API_KEY}

# Set to true in dev/staging to dump extracted products as JSON files to disk.
# Set to false in production to skip file I/O entirely.
scraper.export-json=true

# Directory for JSON exports (only used when export-json=true)
scraper.output-dir=output-json
